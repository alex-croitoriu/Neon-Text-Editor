#pragma once

#define cursorChar ' '

// macros needed for array initialization in main because compiler is retarded

#define windowWidth 1024
#define windowHeight 800
#define maxFontSize 400

extern const int fontUnit;
extern const int timeUnit;
extern const int bucketSize;

extern const int charWidth[400][400];
